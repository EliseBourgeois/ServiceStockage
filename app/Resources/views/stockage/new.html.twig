{% extends 'base.html.twig' %}

{% block body %}
    <h1>Ajout d'un fichier</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('stockage_index') }}">Retour à la liste</a>
        </li>
    </ul>
{% endblock %}
